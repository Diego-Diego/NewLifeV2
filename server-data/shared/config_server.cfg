# This allows players to use scripthook-based plugins such as the legacy Lambda Menu.
# Set this to 1 to allow scripthook. Do note that this does _not_ guarantee players won't be able to use external plugins.
sv_scriptHookAllowed 0

# Uncomment this and set a password to enable RCON. Make sure to change the password - it should look like rcon_password "YOURPASSWORD"
#rcon_password ""

# A comma-separated list of tags for your server.
# For example:
# - sets tags "drifting, cars, racing"
# Or:
# - sets tags "roleplay, military, tanks"
sets tags "default, esx, esx legacy, legacy, nzr, nwx, azr, official, roleplay"

# A valid locale identifier for your server's primary language.
# For example "en-US", "fr-CA", "nl-NL", "de-DE", "en-GB", "pt-BR"
sets locale "fr-FR" 
# please DO replace root-AQ on the line ABOVE with a real language! :)

# Set an optional server info and connecting banner image url.
# Size doesn't matter, any banner sized image will be fine.
#sets banner_detail "https://url.to/image.png"
#sets banner_connecting "https://url.to/image.png"

# Set your server's hostname. This is not usually shown anywhere in listings.
sv_hostname "[DEV] - NzR Extended"

# Set your server's Project Name
sets sv_projectName "NzR Extended"

# Set your server's Project Description
sets sv_projectDesc "dev, nzr."

# Nested configs!
#exec server_internal.cfg

# Loading a server icon (96x96 PNG file)
#load_server_icon myLogo.png

# convars which can be used in scripts
set temp_convar ""

# Remove the `#` from the below line if you want your server to be listed as 'private' in the server browser.
# Do not edit it if you *do not* want your server listed as 'private'.
# Check the following url for more detailed information about this:
# https://docs.fivem.net/docs/server-manual/server-commands/#sv_master1-newvalue
#sv_master1 ""

# enable OneSync (required for server-side state awareness)
## [txAdmin CFG validator]: onesync MUST only be set in the txAdmin settings page.
set onesync on

# Server player slot limit (see https://fivem.net/server-hosting for limits)
sv_maxclients 48

# Steam Web API key, if you want to use Steam authentication (https://steamcommunity.com/dev/apikey)
# -> replace "" with the key
#set steam_webApiKey "F41DE8CDC43FF629DAF3171DA682F856"

# License key for your server (https://keymaster.fivem.net)
sv_licenseKey "cfxk_awLD8cAdfxR3AnZJty2I_1Qibkg"

# Only change the IP if you're using a server with multiple network interfaces, otherwise change the port only.
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# SECURITY
set sv_requestParanoia 3
sv_endpointprivacy true
sv_forceIndirectListing true
#sv_useDirectListing true
sv_authMinTrust 4

# Selects a game build for clients to use.
sv_enforceGameBuild 2802

# SQL connection
set mysql_connection_string "user=root;password=;host=localhost;port=3306;database=local;charset=utf8mb4"

# Add system admins
# makes identifier.license:0c3ea31227c8666028082795ad0274e639c4b197 inherit from group.admin
add_ace group.admin command allow # allow all commands
add_ace group.admin command.quit deny # but don't allow quit
add_principal identifier.license:0c3ea31227c8666028082795ad0274e639c4b197 group.admin


# Start scripts
exec shared/scripts.cfg